# 预测市场公共部分 ER 关系图

```mermaid
erDiagram
  User ||--o{ UserCoin : owns
  User ||--o{ UserCoinLog : generates
  User ||--o{ PredictBet : places
  User ||--o{ PredictUserStat : has
  User ||--o{ PredictUserMarketStat : has
  User ||--o{ Comment : publishes
  User ||--o{ UserLike : creates
  User ||--o{ PredictCampMember : locks
  User ||--o{ PredictCommentRewardItem : receives

  PredictMarket ||--|| PredictContext : exposes
  PredictMarket ||--o{ PredictBet : receives
  PredictMarket ||--o{ PredictUserMarketStat : aggregates
  PredictMarket ||--o{ PredictCommentMeta : binds
  PredictMarket ||--o{ PredictCommentRewardLog : settles_reward
  PredictMarket ||--o{ PredictCommentRewardItem : pays

  PredictBet ||--o| UserCoinLog : stake
  PredictBet ||--o{ PredictUserMarketStat : contributes
  PredictUserStat ||--o{ PredictUserMarketStat : rolls_up

  Comment ||--o| PredictCommentMeta : mapped
  Comment ||--o{ Comment : replies
  Comment ||--o{ UserLike : liked_by
  Comment ||--o{ PredictCommentRewardItem : first_comment

  PredictCommentRewardLog ||--o{ PredictCommentRewardItem : contains
  PredictCommentRewardItem ||--o| UserCoinLog : payout

  PredictCampMember ||--o{ Comment : derives_option
  PredictCampMember ||--o{ PredictBet : locks_option

  PredictMarket {
    int64 id PK
    string sourceModel
    int64 sourceModelId
    string title
    string marketType
    string status
    int64 closeTime
    string result
    int64 baseA
    int64 baseB
    int64 baseDraw
    int64 poolA
    int64 poolB
    int64 poolDraw
  }

  PredictContext {
    int64 id PK
    int64 marketId FK
    string eventName
    string proText
    string conText
    string imageUrl
    string listImage
    string detail
    string tags
    float heat
  }

  PredictBet {
    int64 id PK
    int64 userId FK
    int64 marketId FK
    string option
    int64 amount
    float odds
    string status
    string settleResult
    int64 payout
    int64 settleTime
  }

  UserCoin {
    int64 id PK
    int64 userId FK
    int64 balance
  }

  UserCoinLog {
    int64 id PK
    int64 userId FK
    string bizType
    int64 bizId
    int64 subBizId
    int64 amount
    int64 balanceAfter
  }

  PredictUserStat {
    int64 id PK
    int64 userId FK
    int64 settledMarketCount
    int64 winMarketCount
    int64 loseMarketCount
    float winRate
    int64 currentWinStreak
    int64 bestWinStreak
  }

  PredictUserMarketStat {
    int64 id PK
    int64 userId FK
    int64 marketId FK
    string result
    int64 betAmount
    int64 payout
    int64 settledBetCount
    int64 settleTime
  }

  Comment {
    int64 id PK
    int64 userId FK
    string entityType
    int64 entityId
    string content
  }

  PredictCommentMeta {
    int64 id PK
    int64 commentId FK
    int64 marketId FK
    int64 userId FK
    string optionAtAction
    int64 parentCommentId
    string actionType
  }

  PredictCampMember {
    int64 id PK
    string eventType
    int64 eventId
    int64 topicId
    int64 roundId
    int64 userId FK
    string option
    string lockType
    int64 firstActionTime
  }

  PredictCommentRewardLog {
    int64 id PK
    int64 marketId FK
    string winnerOption
    int64 bonusPool
    float winnerTotalCommentHeat
    int64 remainder
    string status
    string reason
    int64 settledAt
    int64 deadlineAt
    int64 paidAt
  }

  PredictCommentRewardItem {
    int64 id PK
    int64 rewardLogId FK
    int64 marketId FK
    int64 userId FK
    int64 amount
    float userCommentHeat
    int64 firstCommentId
    int64 coinLogId FK
  }

  UserLike {
    int64 id PK
    int64 userId FK
    string entityType
    int64 entityId
    string requestId
  }
```

## 关键约束

- PredictBet：唯一键 `(marketId, userId)`。
- PredictUserMarketStat：唯一键 `(userId, marketId)`。
- PredictCommentMeta：唯一键 `(commentId)`。
- PredictCommentRewardLog：唯一键 `(marketId)`。
- PredictCommentRewardItem：唯一键 `(rewardLogId, userId)`。
- PredictCampMember：唯一键 `(eventType, topicId, roundId, userId)`。
- UserLike：唯一键 `(userId, entityType, entityId)`。

## 跨域关系说明

- PredictMarket 是下注、结算、评论奖励的统一主表。
- PredictContext 仅负责展示，不参与结算。
- PredictBet 同时驱动用户资金、用户战绩和市场池变化。
- PredictCommentMeta 记录评论或回复的动作时选项快照。
- PredictCampMember 记录评论、回复、点赞、下注的阵营锁定关系。
- PredictCommentRewardLog 与 PredictCommentRewardItem 记录开撕台铸币发放审计。
- UserCoinLog 承接下注扣款、下注派奖和评论奖励发币。
- UserLike 只负责点赞去重与查询状态，不承载热度公式。
