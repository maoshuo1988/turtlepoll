# 对立PK：ER关系图

```mermaid
erDiagram
  User ||--o{ PKBet : places
  User ||--o{ PKSettle : settles
  User ||--o{ PKAction : creates
  User ||--o{ UserCoinLog : has
  User ||--o{ UserLike : creates
  User ||--o{ Comment : publishes

  PKTopic ||--o{ PKSeason : has
  PKTopic ||--o{ PKRound : has
  PKTopic ||--o{ PKBet : owns
  PKTopic ||--o{ PKSettle : owns
  PKTopic ||--o{ PKCommentMeta : owns
  PKTopic ||--o{ PKAction : owns

  PKSeason ||--o{ PKRound : includes
  PKRound ||--o{ PKBet : receives
  PKRound ||--o{ PKSettle : generates
  PKRound ||--o{ PKCommentMeta : binds
  PKRound ||--o{ PKAction : records
  PKRound ||--|| T_TEAR_EVENT : maps_to_biz_id
  T_TEAR_EVENT ||--o{ T_TEAR_HEAT_SNAPSHOT : snapshots

  Comment ||--o| PKCommentMeta : mapped
  Comment ||--o{ Comment : replies
  Comment ||--o{ UserLike : liked

  PKBet ||--o| UserCoinLog : stake
  PKSettle ||--o| UserCoinLog : payout
  PKBet ||--o{ PKAction : bet_heat
  PKSettle ||--|| T_TEAR_HEAT_SNAPSHOT : reads_frozen_snapshot

  PKTopic {
    int64 id PK
    string slug
    string title
    string sideAName
    string sideBName
    string status
    int64 currentRoundId FK
    int64 currentSeasonId FK
  }

  PKSeason {
    int64 id PK
    int64 topicId FK
    int seasonNo
    int64 startTime
    int64 endTime
    int winsA
    int winsB
    int totalRounds
    string champion
    string status
  }

  PKRound {
    int64 id PK
    int64 topicId FK
    int64 seasonId FK
    int roundNo
    string phase
    int64 startTime
    int64 lockTime
    int64 endTime
    int64 nextRoundTime
    float heatA
    float heatB
    int64 heatSnapshotTime
    int64 poolA
    int64 poolB
    string winner
  }

  PKBet {
    int64 id PK
    int64 topicId FK
    int64 roundId FK
    int64 userId FK
    string side
    int64 amount
    string requestId
    string settleResult
    int64 payout
  }

  PKSettle {
    int64 id PK
    int64 roundId FK
    int64 userId FK
    string side
    string settleResult
    int64 payout
    int64 snapshotTime
    int64 coinLogId FK
  }

  PKCommentMeta {
    int64 id PK
    int64 commentId FK
    int64 topicId FK
    int64 roundId FK
    string side
    float qualityScore
    float heatScore
  }

  PKAction {
    int64 id PK
    int64 topicId FK
    int64 roundId FK
    int64 userId FK
    string side
    string actionType
    string entityType
    int64 entityId
    int64 amount
    float heat
  }

  T_TEAR_EVENT {
    int64 id PK
    string sceneType
    int64 bizId
    string status
    int64 openTime
    int64 settleTime
    int roundNo
    int seasonNo
  }

  T_TEAR_HEAT_SNAPSHOT {
    int64 id PK
    int64 eventId FK
    string option
    float hLike
    float hComment
    float hCoin
    float hTotal
    int64 snapshotTime
    string snapshotType
  }
```

## 关键约束

- PKBet：唯一键 (roundId, userId)。
- PKSettle：唯一键 (roundId, userId)。
- PKRound：唯一键 (topicId, roundNo)。
- PKSeason：唯一键 (topicId, seasonNo)。
- PKCommentMeta：唯一键 (commentId)。

## 跨域关系说明

- PKRound 通过 `id -> t_tear_event.biz_id` 映射到撕裂带事件，且 `scene_type=ARENA`。
- PKSettle 在结算时读取 `t_tear_heat_snapshot.snapshot_type=SETTLE` 的冻结快照，不直接修改撕裂带快照内容。
- 对立PK仅消费撕裂带快照结果；撕裂带事件与快照仍由撕裂带领域维护。